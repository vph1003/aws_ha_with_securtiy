# Local backend is used by default for this practice project.
# After running ../../../../bootstrap, uncomment this block and initialize with:
#
# terraform init -backend-config=backend-dev.hcl
#
# terraform {
#   backend "s3" {}
# }
